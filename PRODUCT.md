# GitHub Explorer 产品事实

> 版本：1.0
> 日期：2026-08-14（Asia/Shanghai）
> 状态：已确认定位，进入产品设计与实现准备阶段

## 产品定位

GitHub Explorer 是一个 Windows 本地优先的开源项目探索工作台。它帮助用户从 GitHub 项目发现开始，完成可运行性体检、环境搭建、代码理解和隔离二改，并把每一步的证据、失败原因、运行状态和实验结果保存成可复读的项目记录。

它不是另一个泛化的代码聊天框，也不承诺修好所有仓库。核心价值是把陌生仓库变成一个可运行、可解释、可实验和可复盘的本地对象。

## 目标用户

### P0：大学生与初学者

他们需要从项目链接开始，知道项目用途、环境要求、启动入口、失败原因和下一步学习任务。产品默认用人话解释内部结果，但不隐藏可展开的技术证据。

### P1：开发者与研究者

他们需要快速理解陌生仓库、建立基线、修改代码、运行测试、启动服务、比较实验，并保留完整工具调用、命令、路径、进程、变更和验证证据。

### P2：直接体验者

他们只想准备、启动、打开和停止一个项目。产品提供简化的体验入口，不要求用户理解 Python、Node、PowerShell 或 Agent 编排。

## 核心旅程

```text
发现项目 -> 项目体检 -> 准备环境 -> 跑起来
    -> 看懂入口与架构 -> 隔离二改 -> 测试与验证 -> 复盘记录
```

Chat 是贯穿旅程的助手入口，但不是唯一的信息架构。每个项目页应能从同一个事实源投影出项目摘要、执行状态、开发者流水账和实验记录。

## 双层信息原则

### 用户层

默认展示：当前阶段、是否成功、下一步、关键错误、访问地址、变更摘要和验证结论。用户不需要阅读模型原始输出或供应商协议。

### 开发者证据层

同页可展开并筛选：完整工具调用流水账、调用参数、cwd、workspace root、命令和退出码、失败与恢复、文件变更、测试输出、HTTP 检查、受管进程记录、上下文压缩、Session/Turn/Step/Tool call 和本地事件序列。真实进程树与端口归属属于 P4 验收项，未采集到事实时不得伪造。

证据层不是临时调试面板，而是开源项目开发、Harness 测试和产品优化的一等能力。默认折叠只是降低认知负担，不代表删除或丢弃数据。

## 核心承诺

1. 在执行前说明项目、环境、工作区和阶段目标。
2. 在执行后说明做了什么、没做什么、为什么停止，以及证据在哪里。
3. “启动成功”必须关联当前受管进程、端口和版本，而不是仅凭旧页面 HTTP 200。
4. 每次二改都能关联变更、测试、运行结果和失败恢复。
5. 记录可按项目、会话、任务和实验复读，并能导出为 Markdown。

## 非目标

- 不做通用代码补全产品，不与 Cursor、Claude Code、Codex 争夺全部 IDE 场景。
- 不做云端 IDE、生产部署、公网多用户协作或远程执行。
- 暂不做 ISSUE 自动修复、多 Agent 并行和自动发布。
- 不承诺所有仓库一键跑通；失败时提供可解释的诊断和下一步。

## 成功指标

### 用户旅程指标

- 从导入仓库到得到第一条可执行下一步的时间。
- 从导入仓库到确认当前服务身份的时间。
- 环境/路径/端口失败后的平均恢复轮数。
- 首次二改是否同时产生变更证据和验证证据。
- 用户能否在新会话中复读上一次项目状态。

### Harness 指标

- False completion：0。
- 协议泄露：0。
- 无归属进程：0。
- 证据无效率：< 1%。
- 完成需求的有效证据覆盖：100%。

## 架构事实

当前唯一生产编排链是 `LocalAgentRuntime` 的 `Turn -> Step -> Tool pipeline`。SQLite 中的任务、事件、工具调用、审批、变更、验证、进程和记忆记录是唯一事实源；Chat、Activity、Acceptance 和项目记录均为同一事实源的投影。

本地观测覆盖模型请求、工具执行、审批决定、文件变更、验证结果、受管进程和任务终态。模型只负责提出行动和解释结果，Runtime 负责权限、状态机、证据仲裁、失败恢复与终态判定。任何外部 SDK、网络服务或第二套编排图都不参与任务成败。

## 编排与观测决策

- 新旧 JSON、SSE 和 setup 入口统一进入 `LocalAgentRuntime`。
- 旧确认端点因缺少 `task_id` 无法安全映射，返回 `410` 并指向 `/api/agent/approval`。
- 未投入产品使用的多智能体实验端点返回 `410`，不保留第二套状态机。
- 每次模型调用落库开始、完成或失败事件，记录模型、协议、地址、轮次、阶段、延迟、停止原因和 token 用量，但不记录密钥或完整提示词。
- 工具、审批、文件、验证、进程和终态事件与模型事件使用同一任务序列，可按 `task_id` 完整回放。
- 源码门禁禁止重新引入已退役编排和远程观测框架；引入新的编排依赖必须先证明其评测收益和迁移成本。

## 设计原则

- 项目旅程是主线，开发者证据是同页可展开的第二层。
- 事件事实先于模型叙述，模型只补充解释，不决定是否完成。
- 工作区、进程、端口和验证必须有稳定身份。
- 初学者看到下一步，开发者看到证据，二者来自同一个事实源。
- 开源版本保留运行记录能力，支持导出、清理和脱敏，而不是把调试数据硬编码在 UI 中。
