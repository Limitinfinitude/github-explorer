# UI 设计标准（Design Standard）

本文件是 GitHub Explorer 前端的唯一设计标准。所有页面/组件改动必须能回答"这条标准管什么"；
不满足标准的改动视为缺陷，与功能 bug 同级。标准来自真实用户反馈的沉淀，不是风格偏好。

## 1. 人类视角，不是系统视角

用户看的是**结论、语义阶段、下一步**，不是 token 数、事件数、工具名。

- 列表/摘要优先展示结论（完成/失败/卡在哪），过程细节折叠或降级为次要信息；
- 禁止用系统内部计数（token、事件数、调用次数）作为主要信息载体；
- 运行记录按"项目 → 任务 → 结论"组织，时间线按语义阶段（体检/实施/验证）聚合，
  不按事件流平铺。

## 2. 信息层级：一个页面一个中心

- 每个视图必须有明确的中心（hero/主列表），辅助信息（证据、记忆、详情）默认折叠，
  用户需要时才展开；
- 主操作（导入、新建、开始任务）必须一眼可见，不能藏在二级菜单；
- 打开页面 3 秒内说不出"这个页面是干嘛的"，就是设计缺陷。

## 3. 噪音是敌人的一部分

- 内部实现细节（thinking、token、模型请求、schema 修复）默认不进入用户视图；
  诊断模式才显示；
- 同一个语义事实只出现一次，重复的徽章/计数一律删除；
- 空态、加载态、错误态必须明确区分，不允许白屏或无限 spinner。

## 4. 显式优于隐式

- 用户要求过的功能（主题切换、权限档位、钩子配置）必须有显式入口，不能藏在设置里；
- 图标按钮必须有 `title` 和 `aria-label`；
- 危险/不可逆操作（完全访问、删除）必须二次确认。

## 5. 性能是体验的一部分

- 首屏和主要列表接口必须 < 1s（本地数据走 SQL 聚合/批量接口，禁止逐条轮询）；
- 列表数据用 stale-while-revalidate 缓存，轮询只针对"正在运行"的单一实体；
- 任何"加载很慢"的反馈先当设计缺陷处理，再谈功能。

## 6. 一致的状态表达

- 状态用统一的颜色语义：成功（绿）/失败（红）/运行中（蓝或强调色）/等待（灰）；
- 同一概念全站用同一措辞（如"体检"不叫"inspect"，"运行记录"不叫"traces"）；
- 日期时间统一相对时间 + 悬停显示绝对时间。

## 自查清单（改 UI 前过一遍）

- [ ] 这个页面 3 秒内能说出中心是什么？
- [ ] 结论优先了吗？系统计数被当作主信息了吗？
- [ ] 有没有重复/噪音信息？
- [ ] 空态/加载/错误三态都有吗？
- [ ] 新入口是否显式？危险操作有确认吗？
- [ ] 新增的展示是否有对应后端聚合（而不是前端逐条拉取）？

## 修订记录

- 2026-08-24 初版：由工作台/运行记录/探索三轮重构 + 主题切换 + 性能反馈沉淀而来。
