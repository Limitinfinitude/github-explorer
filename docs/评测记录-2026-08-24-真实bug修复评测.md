# 评测记录 — 2026-08-24 真实 bug 修复评测（pdfcpu #1424）

> 目的：验证 agent 能否在**外部判分脚本**下真实修复 GitHub issue（不只是跑通 demo）。
> 样本：pdfcpu（Go 库/CLI），issue #1424（validatePageEntryThumb nil map panic）。
> 关键：对比「无代码指路」vs「有代码指路」的任务描述对 agent 修复能力的影响。

## 1. 判分脚本（外部独立判定）

`checks/pdfcpu/check.py` —— 不依赖 agent 自述，独立验证修复：
- **build**：`go build ./...` 通过（修复不破坏构建）
- **nil_guard_in_source**：`validatePageEntryThumb` 里 PageThumbs 赋值前有 nil 防御（核心断言）
- **runtime_no_panic**：构造 nil PageThumbs + Thumb 引用，`go test` 不 panic
- **environment_diff**：确认改动了源码（防「口头修复」）

**已验证**：未修复 → fail（nil_guard_in_source False）；修复 → pass（4/4）。

## 2. 两轮对比（关键诊断）

| 轮次 | 任务描述 | 结果 | 轮数 | tokens | 诊断 |
|---|---|---|---|---|---|
| fx-1 | 无代码结构指路 | **fail** (incomplete) | 23 | 21508 | agent 在错误路径探索（`pkg/model`/`testdata` 不存在），inspect 阶段预算耗尽未找到 bug |
| fx-2 | 加代码结构指路 | **pass** (completed) | 16 | 3716 | agent 直接定位 `validatePageEntryThumb`，修复成功 |

**fx-2 后 agent 的修复代码**（与基准修复一致，判分 4/4）：
```go
if xRefTable.PageThumbs == nil {
    xRefTable.PageThumbs = map[int]types.IndirectRef{}
}
xRefTable.PageThumbs[xRefTable.CurPage] = *indRef
```

## 3. 关键教训（评测驱动价值）

1. **精确代码结构指路是真实 bug 修复的关键**：
   fx-1 agent 反复搜索 `pkg/model`（实际是 `pkg/pdfcpu/model`）、`testdata`（不存在），
   在错误路径耗尽阶段预算（36 次 inspect 工具）→ 未找到 bug 点。
   fx-2 描述里给出 `pkg/pdfcpu/validate/page.go:276` 精确位置 → agent 立即定位修复。
   **诊断：任务描述=agent 的探路地图，缺指路会让 agent 在未知仓库里盲搜失败。**

2. **代码层断言是最可靠的外部判分**：真实 bug 修复的判分脚本，最可靠的是
   「检查修复点是否落地」（如 nil 防御存在）+ 构建通过，而非构造复杂运行时触发
   （pdfcpu 的 nil map 触发需构造合法 PDF/xref，成本高且不稳定）。

3. **失败痕迹是诊断不是判据**：fx-1 的 tool_failures
   （`search_text 路径不存在 pkg/model`）暴露了 agent 的探索盲区，
   正是改进任务描述的依据。

## 4. 失败模式沉淀

- **探索阶段预算耗尽**：agent 在未知仓库盲搜时，inspect 阶段 36 次工具可能全耗在无效搜索。
  缓解：任务里给「代码结构指路」+ 精确文件/行号。
- **路径猜测错误**：Go 模块仓库常见困惑（`pkg/model` vs `pkg/pdfcpu/model`），
  任务里应明确模块根路径。

## 5. 结论

真实 bug 修复评测闭环验证成功：判分脚本（外部判定）+ 精确任务描述（代码指路）
让 agent 能真实修复 GitHub issue。fx-1（fail）和 fx-2（pass）的对比证明
**任务描述质量直接决定 agent 修复成败**——这是评测驱动开发的又一个可落地洞察。
