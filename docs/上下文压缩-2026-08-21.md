# GitHub Explorer 上下文压缩（2026-08-21）

> 用途：长会话/多会话续接的当前状态快照。读完本文件即可继续工作。

## 1. 项目定位（用户确认）

- **个人学习 + 面试项目**：展示 Agent 架构能力，可观测链（流式/Think/工具卡片/事件账本）是展示台，不能砍。
- 不做商业定位竞争（通用 harness/插件生态打不过 Claude Code 等）；GitHub 项目工具也被沙箱 harness 覆盖。
- 方向探索已收敛：评测方法论 + 失败溯源 + 迭代优化为主轴（用户定的，非功能堆砌）。

## 2. 评测方法论（用户定的流程）

```
失败点逆向溯源（观测链定位，五类归因：环境/工具/harness/模型/任务）
→ 修复 → 复测 → 达到半数通过门槛 → 多轮正式评测（3 轮 2 胜）
→ 失败痕迹累积为复盘资产（不是判据，是诊断）
```
- 判定分级：passed / passed_with_issues / partial / failed（结果优先，失败痕迹降级为诊断）。
- 环境基线（标准变量）：环境快照（版本/网络可达性）、go proxy 基线、端口清理。
- 参照 Terminal-Bench 3 六准则（结果验证而非过程验证）、SWE-bench（FAIL_TO_PASS）。

## 3. 评测历程（完成率）

| 轮次 | 项目集 | 完成率 | 说明 |
|---|---|---|---|
| r1 | 库/框架 10 个 | 2/10 | 基线 |
| r4 | 同上 | 3/10 | 权限模式/轮次 24 |
| r6 | 同上 | 3/10 | 5 并行 |
| r7 | 同上 | **5/10** | 终态仲裁 v2（结果优先），达半数门槛 |
| r8 | 真实应用 10 个（小众） | **0/10**（众数） | 难度分层：修复型 vs 环境型 |

## 4. 已修复清单（harness）

1. 无边界后门关闭（execute/local run/写操作 410）
2. 模型绑定快照（切换模型不改道在飞任务）
3. 乱码修复、_finalize 单点化、模型配置拆模块
4. 边说话边干活（narration/token 实时流）
5. 真流式输出（stream_llm_with_tools，token 逐块）
6. 思考档位 off/high/max（旧值 on/deep 自动迁移）
7. 权限模式三档 confirm/auto/open
8. 终态仲裁 v2：结果优先（成功证据时过程失败降级，无证据严格防误报）
9. repo_map 大仓库惰性遍历（vitest 6.8 万文件卡死修复）
10. reconcile 补 pending 任务（重启 409 修复）
11. 工具超时 180→300、timeout schema 描述
12. **r8 三类修复**：prompt 规则 7（PowerShell 方言）、规则 8（ensure_venv 专用）、max_rounds 24→32
13. 消息落库（chat_messages）、事件归档、SSE 增量查询

## 5. 当前状态

- 服务：`http://127.0.0.1:7788` 运行中（带环境基线 GOPROXY=direct/GONOSUMDB=*）
- 后端测试：308 通过；前端 37 通过
- 评测脚本：`D:\GE-eval-projects\run_eval.py`（v2：reset 预检/auto 审批/parallel/env-snapshot/分级/五类）
- 评测目录：`D:\GE-eval-projects`（10 个真实应用项目，r8 用过）
- 关键文档：
  - `docs/评测记录-2026-08-20-批量项目评测.md`（评测历程/发现/修复）
  - `docs/评测方法论参考-2026-08-21.md`（SWE-bench/Terminal-Bench 方法论）
  - `docs/PRD-v3.0-Agent-Observation-Workbench.md`（方向探索稿，已让位于评测方法论）
  - `docs/ideas-2026-08-20-方向点子.md`（方向点子清单）

## 6. 待办（下一步）

1. **重选项目集**：按新标准（活跃维护/完整应用/适中规模/避免 AI 重依赖），排除 sclack/terraink。
2. **r9 评测**：验证 r8 三类修复（10 并行 + 三轮）。
3. 深挖单个真实应用的完整链路（如 your_spotify 的 tsc 修复循环）验证轮次 32 是否够。
4. 评测脚本 v3 方向：断言清单（Terminal-Bench 式可执行断言）。

## 7. 关键代码位置

- runtime 主循环/终态：`src/agent/runtime/runtime.py`（_drive/_finalize/_stream_reasoning）
- 流式调用：`src/agent/llm.py`（stream_llm_with_tools）
- 权限模式：`src/routes_agent.py`（start approval_mode）+ runtime _execute_active_batch
- 评测脚本：`D:\GE-eval-projects\run_eval.py`
- 环境基线：`run_full.py`（apply_environment_baseline）
